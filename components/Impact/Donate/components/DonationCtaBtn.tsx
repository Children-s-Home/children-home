'use client'
import PropTypes from "prop-types";

export default function DonationCtaBtn({ children }:any) {
  return <button className="donation-cta">{children}</button>;
}

DonationCtaBtn.propTypes = {};

